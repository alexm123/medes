% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/upsilons_cov.R
\name{upsilons_cov}
\alias{upsilons_cov}
\title{Upsilon from covariance matrix only}
\usage{
upsilons_cov(
  S,
  x,
  m,
  y,
  n,
  R = 1000,
  ci_level = 0.95,
  do_bootstrap = TRUE,
  stat = c("adjusted", "unadjusted", "both")
)
}
\arguments{
\item{S}{A 3x3 covariance matrix whose row/col names include x, m, y.
Order can be arbitrary but names must be present.}

\item{x, m, y}{Character names of the variables in S.}

\item{n}{Integer, sample size used to compute S (number of observations).}

\item{R}{Number of bootstrap draws (default 1000).}

\item{ci_level}{Confidence level for intervals (default 0.95).}

\item{do_bootstrap}{Logical; if TRUE, run parametric bootstrap (default TRUE).}

\item{stat}{One of "adjusted", "unadjusted", or "both" (default "both").}
}
\value{
A named list with point estimates and (optionally) CIs.
}
\description{
Compute unadjusted and adjusted upsilon mediation measures using only a
covariance matrix (of x, m, y) and the sample size n. Optionally, obtain
bootstrap confidence intervals via parametric bootstrap (MVN simulation).
}
\examples{
set.seed(42)
# Fake covariance with var(x)=1, var(m)=1, var(y)=1.5, some covariances
S <- matrix(c(
  1.00, 0.30, 0.25,
  0.30, 1.00, 0.40,
  0.25, 0.40, 1.50
), 3, 3, byrow = TRUE)
dimnames(S) <- list(c("x","m","y"), c("x","m","y"))
upsilons_cov(S, x="x", m="m", y="y", n=500, R=500, do_bootstrap=FALSE)
}
