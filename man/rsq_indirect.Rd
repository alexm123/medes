% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rsquare_ind.R
\name{rsq_indirect}
\alias{rsq_indirect}
\title{Function to compute de Heus' R squared Indirect}
\usage{
rsq_indirect(data, x, m, y, R = 5000, ci_level = 0.95, do_bootstrap = TRUE)
}
\arguments{
\item{data}{A data.frame containing the variables.}

\item{x, m, y}{Character names of the X (predictor), M (mediator), and Y (outcome) columns in \code{data}.}

\item{R}{Integer, bootstrap replications. Default 5000.}

\item{ci_level}{Confidence level for the percentile CI. Default 0.95.}

\item{do_bootstrap}{Logical; if \code{TRUE}, returns bootstrap percentile CI.}
}
\value{
If \code{do_bootstrap = FALSE}, a single numeric \code{rsq_ind}.
If \code{TRUE}, a list with \code{rsq_ind_lower}, \code{rsq_ind}, and \code{rsq_ind_upper}.
}
\description{
Computes the de Heus R^2 for an indirect (mediation) pathway with optional
percentile bootstrap CIs.
}
\examples{
\dontrun{
  set.seed(42)
  n <- 200
  x <- rnorm(n, mean = 50, sd = 10)
  m  <- 0.5*x + rnorm(n, mean = 0, sd = 5)
  y <- 0.3*x + 0.6*m + rnorm(n, mean = 0, sd = 5)
  d <- data.frame(x, m, y)
  rsq_ind(d, "x", "m", "y", R = 1000)
}

}
