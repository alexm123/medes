% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/upsilons.R, R/upsilons_sem_ols.R
\name{upsilons}
\alias{upsilons}
\title{Upsilon Mediation Effect Estimates via SEM and/or OLS}
\usage{
upsilons(
  data,
  x,
  m,
  y,
  engine = c("both", "sem", "ols"),
  R = 5000,
  ci_level = 0.95,
  do_bootstrap = TRUE
)

upsilons(
  data,
  x,
  m,
  y,
  engine = c("both", "sem", "ols"),
  R = 5000,
  ci_level = 0.95,
  do_bootstrap = TRUE
)
}
\arguments{
\item{data}{A \code{data.frame} containing the variables of interest.}

\item{x}{Character string. Name of the independent (predictor) variable.}

\item{m}{Character string. Name of the mediator variable.}

\item{y}{Character string. Name of the dependent (outcome) variable.}

\item{engine}{Character. One of \code{"ols"}, \code{"sem"}, or \code{"both"} (default).
Controls which estimator(s) are computed and, if bootstrapping,
which bootstrap(s) are run.}

\item{R}{Integer. Number of bootstrap replications. Default is 5000.}

\item{ci_level}{Numeric. Confidence level for bootstrap intervals
(between 0 and 1). Default is 0.95.}

\item{do_bootstrap}{Logical. Whether to perform bootstrapping
to compute confidence intervals. Default is \code{TRUE}.}
}
\value{
A named \code{list} containing some or all of:
\describe{
\item{upsilon_sem}{Point estimate of Upsilon from SEM.}
\item{adj.upsilon_sem}{Adjusted Upsilon from SEM.}
\item{ci.upsilon_sem}{Bootstrap CI for SEM Upsilon (if bootstrapped).}
\item{ci.adj.upsilon_sem}{Bootstrap CI for adjusted SEM Upsilon (if bootstrapped).}
\item{upsilon_ols}{Point estimate of Upsilon from OLS.}
\item{adj.upsilon_ols}{Adjusted Upsilon from OLS.}
\item{ci.upsilon_ols}{Bootstrap CI for OLS Upsilon (if bootstrapped).}
\item{ci.adj.upsilon_ols}{Bootstrap CI for adjusted OLS Upsilon (if bootstrapped).}
}

A named \code{list} containing some or all of:
\describe{
\item{upsilon_sem}{Point estimate of Upsilon from SEM.}
\item{adj.upsilon_sem}{Adjusted Upsilon from SEM.}
\item{ci.upsilon_sem}{Bootstrap CI for SEM Upsilon (if bootstrapped).}
\item{ci.adj.upsilon_sem}{Bootstrap CI for adjusted SEM Upsilon (if bootstrapped).}
\item{upsilon_ols}{Point estimate of Upsilon from OLS.}
\item{adj.upsilon_ols}{Adjusted Upsilon from OLS.}
\item{ci.upsilon_ols}{Bootstrap CI for OLS Upsilon (if bootstrapped).}
\item{ci.adj.upsilon_ols}{Bootstrap CI for adjusted OLS Upsilon (if bootstrapped).}
}
}
\description{
Computes the Upsilon statistic for mediation effects using
Structural Equation Modeling (SEM, via \pkg{lavaan}) and/or
Ordinary Least Squares (OLS). Optionally, percentile bootstrap
confidence intervals are provided for the chosen engine(s).

Computes the Upsilon statistic for mediation effects using
Structural Equation Modeling (SEM, via \pkg{lavaan}) and/or
Ordinary Least Squares (OLS). Optionally, percentile bootstrap
confidence intervals are provided for the chosen engine(s).
}
\details{
The function fits a simple mediation model with one mediator using
SEM (\pkg{lavaan}) and/or two OLS regressions. Upsilon statistics
(original and adjusted) are computed from path coefficients and covariance.

If \code{do_bootstrap = TRUE}:
\itemize{
\item SEM bootstrap is performed via \code{\link[lavaan:bootstrap]{lavaan::bootstrapLavaan()}} (if \code{engine} includes \code{"sem"}).
\item OLS bootstrap uses row-resampling (pairs bootstrap) (if \code{engine} includes \code{"ols"}).
}

The returned list includes only elements for the requested \code{engine}.

The function fits a simple mediation model with one mediator using
SEM (\pkg{lavaan}) and/or two OLS regressions. Upsilon statistics
(original and adjusted) are computed from path coefficients and covariance.

If \code{do_bootstrap = TRUE}:
\itemize{
\item SEM bootstrap is performed via \code{\link[lavaan:bootstrap]{lavaan::bootstrapLavaan()}} (if \code{engine} includes \code{"sem"}).
\item OLS bootstrap uses row-resampling (pairs bootstrap) (if \code{engine} includes \code{"ols"}).
}

The returned list includes only elements for the requested \code{engine}.
}
\examples{
\dontrun{
set.seed(123)
d <- data.frame(x = rnorm(100), m = rnorm(100), y = rnorm(100))

# OLS only
res_ols <- upsilons(d, x="x", m="m", y="y", engine="ols",
                            do_bootstrap=FALSE)

# SEM only with bootstrap
res_sem <- upsilons(d, x="x", m="m", y="y", engine="sem",
                            do_bootstrap=TRUE, R=1000)

# Both (default)
res_both <- upsilons(d, x="x", m="m", y="y")
}

\dontrun{
set.seed(123)
d <- data.frame(x = rnorm(100), m = rnorm(100), y = rnorm(100))

# OLS only
res_ols <- upsilons(d, x="x", m="m", y="y", engine="ols",
                            do_bootstrap=FALSE)

# SEM only with bootstrap
res_sem <- upsilons(d, x="x", m="m", y="y", engine="sem",
                            do_bootstrap=TRUE, R=1000)

# Both (default)
res_both <- upsilons(d, x="x", m="m", y="y")
}

}
